import styled from 'styled-components';

export const Container = styled.main`
	max-width: 1120px;
	margin: 0 auto;
	padding: 2.5rem 1rem;

	.ant-layout {
		background-color: rgba(0, 0, 0, 0);
	}
	.ant-layout-sider {
		background-color: rgba(0, 0, 0, 0);
	}
	.ant-menu {
		background-color: rgba(0, 0, 0, 0);
	}
`;
